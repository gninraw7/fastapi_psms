# PSMS 시스템 아키텍처

## 🏗️ 3-Tier 아키텍처 구조

```
┌─────────────────────────────────────────────────────────────┐
│                      Presentation Layer                      │
│  ┌─────────────────────────────────────────────────────┐    │
│  │              Excel VBA UserForm                      │    │
│  │  ┌──────────────┐  ┌──────────────┐  ┌───────────┐ │    │
│  │  │ FrmProject   │  │  ComboBoxes  │  │   iGrid   │ │    │
│  │  │   List       │  │  (FIELD,     │  │  Control  │ │    │
│  │  │              │  │   STAGE,     │  │           │ │    │
│  │  │              │  │   MANAGER)   │  │           │ │    │
│  │  └──────┬───────┘  └──────┬───────┘  └─────┬─────┘ │    │
│  │         │                 │                 │        │    │
│  │         └─────────────────┼─────────────────┘        │    │
│  │                           │                          │    │
│  │                    ┌──────▼──────┐                   │    │
│  │                    │ ModFastAPI  │                   │    │
│  │                    │  (HTTP 요청) │                   │    │
│  │                    └──────┬──────┘                   │    │
│  └───────────────────────────┼──────────────────────────┘    │
└────────────────────────────┼─────────────────────────────────┘
                             │ HTTP/JSON
                             │ (REST API)
┌────────────────────────────▼─────────────────────────────────┐
│                     Application Layer                         │
│  ┌────────────────────────────────────────────────────────┐  │
│  │              FastAPI Server (Python)                    │  │
│  │  ┌──────────────────────────────────────────────────┐  │  │
│  │  │           API Endpoints (routes.py)              │  │  │
│  │  │  GET  /api/v1/projects/list                      │  │  │
│  │  │  GET  /api/v1/projects/combo/{code}              │  │  │
│  │  │  GET  /api/v1/projects/managers                  │  │  │
│  │  └────────────────────┬─────────────────────────────┘  │  │
│  │                       │                                 │  │
│  │  ┌────────────────────▼─────────────────────────────┐  │  │
│  │  │         Services (project_service.py)            │  │  │
│  │  │  • get_project_list()  (페이징, 필터링)          │  │  │
│  │  │  • get_combo_data()                              │  │  │
│  │  │  • get_managers()                                │  │  │
│  │  └────────────────────┬─────────────────────────────┘  │  │
│  │                       │                                 │  │
│  │  ┌────────────────────▼─────────────────────────────┐  │  │
│  │  │         Schemas (Pydantic Models)                │  │  │
│  │  │  • ProjectListRequest                            │  │  │
│  │  │  • ProjectListResponse                           │  │  │
│  │  │  • ComboResponse                                 │  │  │
│  │  └──────────────────────────────────────────────────┘  │  │
│  │                                                         │  │
│  │  ┌──────────────────────────────────────────────────┐  │  │
│  │  │     Core (database.py, config.py)                │  │  │
│  │  │  • SQLAlchemy Engine                             │  │  │
│  │  │  • Session Management                            │  │  │
│  │  │  • Configuration                                 │  │  │
│  │  └────────────────────┬─────────────────────────────┘  │  │
│  └─────────────────────────┼────────────────────────────────┘  │
└────────────────────────────┼───────────────────────────────────┘
                             │ SQL Queries
                             │ (SQLAlchemy + PyMySQL)
┌────────────────────────────▼───────────────────────────────────┐
│                        Data Layer                              │
│  ┌──────────────────────────────────────────────────────────┐ │
│  │                    MySQL Database                         │ │
│  │  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐   │ │
│  │  │   projects   │  │  comm_code   │  │    users     │   │ │
│  │  │              │  │              │  │              │   │ │
│  │  │ pipeline_id  │  │ group_code   │  │  login_id    │   │ │
│  │  │ project_name │  │ code         │  │  user_name   │   │ │
│  │  │ customer     │  │ code_name    │  │ is_sales_rep │   │ │
│  │  │ field        │  │ sort_order   │  │  status      │   │ │
│  │  │ stage        │  │ is_use       │  │              │   │ │
│  │  │ manager_id   │  │              │  │              │   │ │
│  │  │ amount       │  │              │  │              │   │ │
│  │  │ probability  │  │              │  │              │   │ │
│  │  │ expected_date│  │              │  │              │   │ │
│  │  └──────────────┘  └──────────────┘  └──────────────┘   │ │
│  └──────────────────────────────────────────────────────────┘ │
└────────────────────────────────────────────────────────────────┘
```

## 🔄 데이터 흐름

### 1. 프로젝트 목록 조회 흐름

```
[VBA UserForm]
    │
    ├─► LoadGridData() 호출
    │       │
    │       ├─► params 구성 (page, page_size, filters)
    │       │
    │       └─► ModFastAPI.HttpGet("/projects/list", params)
    │
    ▼
[ModFastAPI]
    │
    ├─► URL 구성: http://localhost:8000/api/v1/projects/list?page=1&page_size=25
    │
    ├─► MSXML2.XMLHTTP로 GET 요청
    │
    └─► JSON 응답을 VBA Dictionary로 파싱
    │
    ▼
[FastAPI Server]
    │
    ├─► routes.py: get_project_list() 엔드포인트
    │       │
    │       └─► ProjectListRequest 스키마로 파라미터 검증
    │
    ├─► project_service.py: get_project_list() 비즈니스 로직
    │       │
    │       ├─► WHERE 조건 생성 (검색, 필터)
    │       │
    │       ├─► COUNT 쿼리로 총 레코드 수 조회
    │       │
    │       ├─► 페이징 쿼리 (LIMIT, OFFSET)
    │       │
    │       └─► ProjectListResponse 반환
    │
    ▼
[MySQL Database]
    │
    ├─► SELECT COUNT(*) FROM projects WHERE ...
    │
    └─► SELECT * FROM projects ... LIMIT 25 OFFSET 0
    │
    ▼
[FastAPI Response]
    │
    └─► JSON Response:
        {
          "total_records": 150,
          "total_pages": 6,
          "current_page": 1,
          "page_size": 25,
          "items": [...]
        }
    │
    ▼
[VBA Processing]
    │
    ├─► mTotalRecords, mTotalPages 업데이트
    │
    ├─► iGrid에 데이터 채우기
    │
    └─► 페이지 정보 표시
```

### 2. 콤보박스 데이터 로드 흐름

```
[VBA] SetComboFromAPI("FIELD")
  │
  └─► ModFastAPI.HttpGet("/projects/combo/FIELD")
      │
      ▼
[FastAPI] GET /api/v1/projects/combo/FIELD
  │
  └─► ProjectService.get_combo_data(db, "FIELD")
      │
      └─► SELECT * FROM comm_code 
          WHERE group_code = 'FIELD' 
          AND is_use = 'Y' 
          ORDER BY sort_order
      │
      ▼
[Response] 
{
  "group_code": "FIELD",
  "items": [
    {"code": "IT", "code_name": "IT/시스템"},
    {"code": "CONSULTING", "code_name": "컨설팅"}
  ]
}
  │
  ▼
[VBA] ComboBox에 항목 추가
```

## 📦 폴더 구조 상세

```
fastapi_psms/
│
├── app/                          # 애플리케이션 코드
│   ├── api/                      # API 레이어
│   │   └── v1/                   # API 버전 1
│   │       ├── endpoints/        # 엔드포인트 모음
│   │       │   ├── projects/     # 프로젝트 관련
│   │       │   │   └── routes.py # 프로젝트 API
│   │       │   ├── common_codes/ # 공통코드 (향후)
│   │       │   └── users/        # 사용자 (향후)
│   │       └── api.py            # 라우터 통합
│   │
│   ├── core/                     # 핵심 모듈
│   │   ├── config.py             # 설정 (DB, API 등)
│   │   └── database.py           # DB 연결, 세션
│   │
│   ├── models/                   # ORM 모델 (향후)
│   ├── schemas/                  # Pydantic 스키마
│   │   └── project.py            # 프로젝트 스키마
│   │
│   ├── services/                 # 비즈니스 로직
│   │   └── project_service.py    # 프로젝트 서비스
│   │
│   └── utils/                    # 유틸리티 함수
│
├── vba_modules/                  # VBA 코드
│   ├── ModFastAPI.bas            # HTTP 요청 모듈
│   └── FrmProjectList_FastAPI.frm # UserForm
│
├── logs/                         # 로그 파일
├── tests/                        # 테스트 코드
│
├── .env                          # 환경 변수
├── main.py                       # 앱 진입점
├── requirements.txt              # Python 패키지
├── test_data.sql                 # 테스트 데이터
├── start_server.bat              # 실행 배치
├── README.md                     # 프로젝트 문서
└── 빠른시작가이드.md              # 시작 가이드
```

## 🔐 보안 및 확장성

### 현재 구현
- ✅ 3-Tier 분리 아키텍처
- ✅ RESTful API 설계
- ✅ Pydantic 데이터 검증
- ✅ SQLAlchemy 연결 풀
- ✅ CORS 설정

### 향후 확장 계획
- 🔲 JWT 인증/인가
- 🔲 API Rate Limiting
- 🔲 로깅 및 모니터링
- 🔲 캐싱 (Redis)
- 🔲 Docker 컨테이너화
- 🔲 CI/CD 파이프라인
