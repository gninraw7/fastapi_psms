# FastAPI 서버 빠른 시작 가이드

## 📦 프로젝트 설치

### 1단계: 압축 해제
다운로드한 `fastapi_psms.tar.gz` 파일을 원하는 위치에 압축 해제합니다.

예: `C:\Users\KBDS\fastapi_psms`

### 2단계: 가상환경 생성 및 패키지 설치

CMD 또는 PowerShell을 열고:

```cmd
cd C:\Users\KBDS\fastapi_psms
python -m venv venv
venv\Scripts\activate
pip install -r requirements.txt
```

### 3단계: 데이터베이스 설정

`.env` 파일을 열어 MySQL 연결 정보를 수정:

```env
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=your_password
DB_NAME=psms_db
```

### 4단계: 테스트 데이터 입력

MySQL에 접속하여 `test_data.sql` 실행:

```sql
mysql -u root -p < test_data.sql
```

또는 MySQL Workbench에서 직접 실행

### 5단계: 서버 실행

**방법 1: 배치 파일 사용 (권장)**
```cmd
start_server.bat
```

**방법 2: 직접 실행**
```cmd
venv\Scripts\activate
uvicorn main:app --reload --host 0.0.0.0 --port 8000
```

### 6단계: 연결 확인

브라우저에서 다음 URL 접속:
- API 문서: http://localhost:8000/docs
- Health Check: http://localhost:8000/health

## 🔧 VBA Excel 설정

### 1. VBA-JSON 라이브러리 설치

1. https://github.com/VBA-tools/VBA-JSON/releases 에서 최신 버전 다운로드
2. Excel을 열고 Alt+F11로 VBA 편집기 열기
3. 파일 → 가져오기 → `JsonConverter.bas` 선택

### 2. 참조 추가

VBA 편집기에서:
1. 도구 → 참조 클릭
2. 다음 항목 체크:
   - ✅ Microsoft Scripting Runtime
   - ✅ Microsoft XML, v6.0

### 3. 모듈 가져오기

1. `vba_modules` 폴더의 `ModFastAPI.bas` import
2. 기존 `FrmProjectList` 백업 후 `FrmProjectList_FastAPI.frm` 내용으로 교체

### 4. 연결 테스트

VBA 즉시 실행 창(Ctrl+G)에서:
```vb
? ModFastAPI.TestConnection()
```

"FastAPI 서버 연결 성공!" 메시지가 나오면 정상입니다.

## 📊 주요 API 엔드포인트

### 프로젝트 목록 조회
```
GET /api/v1/projects/list?page=1&page_size=25
```

### 콤보박스 데이터 (FIELD)
```
GET /api/v1/projects/combo/FIELD
```

### 콤보박스 데이터 (STAGE)
```
GET /api/v1/projects/combo/STAGE
```

### 담당자 목록
```
GET /api/v1/projects/managers
```

## 🐛 문제 해결

### 서버가 시작되지 않을 때
1. Python 버전 확인 (3.8 이상 필요)
2. 포트 8000이 이미 사용 중인지 확인
3. 가상환경이 활성화되어 있는지 확인

### DB 연결 오류
1. MySQL 서버가 실행 중인지 확인
2. `.env` 파일의 DB 정보가 정확한지 확인
3. 방화벽에서 3306 포트 허용 확인

### VBA에서 연결 안 될 때
1. FastAPI 서버가 실행 중인지 확인
2. VBA-JSON 라이브러리가 설치되었는지 확인
3. 참조 라이브러리가 모두 체크되었는지 확인

## 💡 개발 팁

### API 문서 활용
http://localhost:8000/docs 에서 모든 API를 테스트할 수 있습니다.
"Try it out" 버튼으로 직접 요청을 보내보세요.

### 로그 확인
서버 실행 중인 CMD 창에서 실시간 로그를 확인할 수 있습니다.

### 코드 수정 시
`--reload` 옵션으로 실행했다면 코드 수정 시 자동으로 서버가 재시작됩니다.

## 📞 지원

추가 문의사항은 KBDS IT팀으로 연락주세요.
